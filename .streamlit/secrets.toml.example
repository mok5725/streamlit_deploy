# 이 파일을 복사해 `secrets.toml`로 저장한 뒤 아래 값을 채우세요.
# `secrets.toml`은 Git에 올리지 마세요 (.gitignore에 포함됨).
#
# Streamlit Community Cloud에서는 웹 UI:
#   앱 → Settings → Secrets 에서 아래와 같이 입력:
#
# OPENAI_API_KEY = "sk-..."

OPENAI_API_KEY = "sk-your-key-here"
